# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# server: SQLite data files (never commit user data)
server/data.db
server/data.db-shm
server/data.db-wal
# docker bind-mount data folder (SQLite file lives here when run via compose)
server/data/

# logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store

# local Claude Code settings (launch.json is kept/shared)
.claude/settings.local.json

# local data backups (personal content — never commit)
/backups/

# AI session transcripts (not for publishing)
session-ses_*.md

# local tooling artifacts (editor workspace, MCP config, Playwright debug output)
cortex.code-workspace
opencode.json
.playwright-mcp/
